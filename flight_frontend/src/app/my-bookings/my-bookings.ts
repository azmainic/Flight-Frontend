/**
 * MyBookings component displays the user's flight bookings.
 * It retrieves booking data from the backend and presents it in a user-friendly format.
 * The component allows users to view details of their bookings and manage them as needed.
 */

import { Component } from '@angular/core';

/**
 * MyBookings class defines the structure and behavior of the My Bookings page in the application.
 * It is responsible for displaying the user's flight bookings and providing functionality to manage those bookings.
 * The component will interact with services to fetch booking data and handle user actions related to their bookings.
 */

@Component({
  selector: 'app-my-bookings',
  imports: [],
  templateUrl: './my-bookings.html',
  styleUrl: './my-bookings.css',
})
export class MyBookings {}
