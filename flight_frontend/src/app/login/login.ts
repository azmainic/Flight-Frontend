/**
 * Login component serves as the user interface for user authentication.
 * It provides a form for users to enter their credentials and handles the login process.
 * The component interacts with AuthService to perform authentication and manage user sessions.
 * It also includes error handling to display appropriate messages for failed login attempts.
 */

import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  imports: [],
  templateUrl: './login.html',
  styleUrl: './login.css',
})
export class Login {}
